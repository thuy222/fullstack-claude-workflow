# Copy to .env and adjust. Postgres connection string used by Prisma + NestJS.
# Local dev (Postgres from docker-compose): host=localhost
# Inside Docker (api container): host=db (the compose service name)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/app?schema=public"
PORT=3001
