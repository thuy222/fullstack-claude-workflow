{
  "name": "next-claude-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo: Next.js client + NestJS server. Each side installs and deploys independently.",
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm:dev:client\" \"npm:dev:server\"",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run start:dev",
    "build": "npm run build:client && npm run build:server",
    "build:client": "npm --prefix client run build",
    "build:server": "npm --prefix server run build",
    "lint": "npm --prefix client run lint && npm --prefix server run lint",
    "install:all": "npm install && npm --prefix client install && npm --prefix server install",
    "db:up": "docker compose -f server/docker-compose.yml up -d db",
    "db:down": "docker compose -f server/docker-compose.yml down"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
